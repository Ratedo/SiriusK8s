![]()
скриншот доказывает, что все мои три контейнера работают (статус healthy). 
![]()
тут я кароч сделала запрос и получила JSON с данными из базы. Это также доказывает, что ВСЯ цепочка работает. 
![]()
запуск стека из трех сервисов также гууд
db - база данных
backend (Flask) — бэкенд приложение  
frontend (nginx) — веб-сервер на порту 8080
ВЫВОД: 
я научилась описывать приложения причем не с одним контейнером так еще и в YAML файле.

столкнулась с ошибкой, которая связана с табуляцией, впредь, буду обращать пристальное внимание на нее.

